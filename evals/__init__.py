"""Opt-in LLM evals (outside the hermetic unit suite in `tests/`).

A package so the eval modules can share one hand-verified corpus (`cases.py`)
across the structural grounding checks and the cross-vendor LLM-as-judge.
"""
